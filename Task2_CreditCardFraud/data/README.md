# Dataset

The `creditcard.csv` dataset is not included because it exceeds GitHub's file size limit.

You can download it from:
https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud

Place `creditcard.csv` inside this `data` folder before running the notebook.